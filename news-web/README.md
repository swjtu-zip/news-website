# News web fixtures

这里集中存放用于采集器解析回归测试和人工核对的原始 HTML 快照。它们不是归档服务的运行时输入；运行时代码位于 [`../swjtu-archive/`](../swjtu-archive/)，采集器位于相邻的 [`../../SWJTU-CLI/`](../../SWJTU-CLI/) 仓库。

文件名中的 `list1`、`list2` 表示抓取时保存的列表页样本。新增快照请继续放在本目录，避免把临时网页文件散落在项目根目录。
